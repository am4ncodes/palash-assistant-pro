/*
 * Feature module: authenticated profile and durable progress overview.
 * Auth state comes from the scaffold's Manus OAuth hook; persistence comes from tRPC.
 */
import { useState } from "react";
import { BookMarked, Check, LogIn, LogOut, RefreshCw, UserRound } from "lucide-react";
import { toast } from "sonner";
import AppShell from "@/components/AppShell";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { useAppStore } from "@/contexts/AppStore";

export default function ProfilePage() {
  const auth = useAuth();
  const { completedLessonIds, savedPhrases } = useAppStore();
  const progressQuery = trpc.progress.get.useQuery(undefined, { enabled: auth.isAuthenticated });
  const updateProfile = trpc.profile.update.useMutation();
  const [name, setName] = useState("");

  if (!auth.isAuthenticated) return <AppShell><main className="mx-auto max-w-[1380px] px-5 py-20 sm:px-8 lg:px-12"><div className="mx-auto max-w-xl rounded-[28px] border border-[#20251f]/10 bg-white p-8 text-center shadow-[0_16px_50px_rgba(32,37,31,0.07)]"><div className="mx-auto grid size-16 place-items-center rounded-full bg-[#e8f1e5] text-[#2d6b4a]"><UserRound size={25} /></div><div className="section-eyebrow mt-7 justify-center">Profile / sign in</div><h1 className="font-display text-5xl leading-none">Keep your progress<br /><span className="text-[#e45d32]">with you.</span></h1><p className="mt-5 text-sm leading-6 text-[#6b746a]">Sign in to sync saved phrases, lesson progress, and your downloaded library across sessions.</p><button onClick={() => startLogin()} className="mt-8 inline-flex items-center gap-2 rounded-full bg-[#20251f] px-5 py-3.5 text-sm font-semibold text-white hover:bg-[#2d6b4a]"><LogIn size={16} /> Sign in with Manus</button></div></main></AppShell>;

  const serverProgress = progressQuery.data;
  const serverCompleted = serverProgress ? JSON.parse(serverProgress.completedPhrases || "[]") as number[] : completedLessonIds;
  const serverSaved = serverProgress?.savedPhrases ?? savedPhrases;

  return <AppShell><main className="mx-auto max-w-[1380px] px-5 py-12 sm:px-8 lg:px-12 lg:py-20"><div className="flex flex-wrap items-end justify-between gap-8"><div><div className="section-eyebrow">Profile / teacher mode</div><h1 className="font-display text-6xl leading-[0.9] tracking-[-0.05em] sm:text-8xl">Your field<br /><span className="text-[#e45d32]">notes.</span></h1><p className="mt-7 max-w-[470px] text-[16px] leading-7 text-[#566055]">A personal record of the phrases you’ve saved and the classroom practice you’ve completed.</p></div><button onClick={() => auth.logout().then(() => toast.success("Signed out"))} className="inline-flex items-center gap-2 rounded-full border border-[#20251f]/15 px-4 py-3 text-sm font-semibold hover:border-[#e45d32] hover:text-[#e45d32]"><LogOut size={15} /> Sign out</button></div><div className="mt-12 grid gap-5 lg:grid-cols-[0.75fr_1.25fr]"><section className="rounded-[28px] bg-[#2d6b4a] p-7 text-[#f8f2e8] sm:p-9"><div className="field-stamp field-stamp--light">PS</div><div className="mt-12 font-mono text-[10px] uppercase tracking-[0.16em] text-[#f8f2e8]/55">Signed-in teacher</div><div className="mt-2 font-display text-4xl">{auth.user?.name || "Teacher"}</div><div className="mt-2 text-sm text-[#f8f2e8]/60">{auth.user?.email || "Connected via Manus OAuth"}</div><div className="mt-10 border-t border-[#f8f2e8]/15 pt-5 font-mono text-[10px] uppercase tracking-[0.12em] text-[#f8f2e8]/55">Account state</div><div className="mt-2 flex items-center gap-2 text-sm font-semibold"><span className="size-2 rounded-full bg-[#f1b36d]" /> Cloud sync enabled</div></section><section className="ruled-surface rounded-[28px] border border-[#20251f]/10 bg-white p-7 sm:p-9"><div className="flex items-start justify-between gap-5"><div><div className="font-mono text-[10px] uppercase tracking-[0.16em] text-[#6b746a]">Learning ledger</div><h2 className="mt-2 font-display text-4xl">Progress that travels.</h2></div><RefreshCw size={18} className={progressQuery.isFetching ? "animate-spin text-[#e45d32]" : "text-[#6b746a]"} /></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-[#e8f1e5] p-5"><Check size={18} className="text-[#2d6b4a]" /><div className="mt-6 font-display text-4xl">{serverCompleted.length}</div><div className="mt-1 font-mono text-[9px] uppercase tracking-[0.12em] text-[#6b746a]">Phrases learned</div></div><div className="rounded-2xl bg-[#fff0e7] p-5"><BookMarked size={18} className="text-[#e45d32]" /><div className="mt-6 font-display text-4xl">{serverSaved}</div><div className="mt-1 font-mono text-[9px] uppercase tracking-[0.12em] text-[#6b746a]">Saved phrases</div></div><div className="rounded-2xl bg-[#d8def1] p-5"><RefreshCw size={18} className="text-[#313d63]" /><div className="mt-6 font-display text-4xl">{serverProgress ? "Live" : "Local"}</div><div className="mt-1 font-mono text-[9px] uppercase tracking-[0.12em] text-[#6b746a]">Sync state</div></div></div><div className="mt-8 border-t border-[#20251f]/10 pt-6"><label htmlFor="profile-name" className="font-mono text-[10px] uppercase tracking-[0.14em] text-[#6b746a]">Preferred display name</label><div className="mt-3 flex flex-col gap-3 sm:flex-row"><input id="profile-name" value={name || auth.user?.name || ""} onChange={(event) => setName(event.target.value)} className="min-w-0 flex-1 rounded-full border border-[#20251f]/15 bg-[#f8f2e8] px-4 py-3 text-sm outline-none focus:border-[#e45d32]" /><button onClick={() => { const nextName = name || auth.user?.name || "Teacher"; updateProfile.mutate({ name: nextName }, { onSuccess: () => toast.success("Profile updated") }); }} className="rounded-full bg-[#20251f] px-5 py-3 text-sm font-semibold text-white hover:bg-[#2d6b4a]">Save profile</button></div></div></section></div></main></AppShell>;
}
